# calcPSF
Calculate photoemission (or photoelectron) structure factor (PSF)

##



# Note
Clone on the shared drive
```
sudo git -c core.sshCommand="ssh -i /path/to/key/file" clone git@github.com:Hiroaki-Tanaka-0606/calcPSF.git
```
